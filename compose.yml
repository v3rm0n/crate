services:
  crate:
    build: .
    container_name: crate
    ports:
      - "3000:3000"
    volumes:
      - crate-data:/data
      - /Volumes/music:/library:ro
      - /Volumes/IPOD:/player
    restart: unless-stopped

volumes:
  crate-data:
